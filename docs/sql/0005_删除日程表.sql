-- ============================================================
-- MyCal v0.4.1 · 日程功能退役 —— 远端 D1 脚本
-- 请在 Cloudflare 网页 D1 Console(mycal_db)执行。
-- 删除 todos 表(手动待办与 ICS 日程的载体,均已弃用)。幂等,可重复执行。
-- 数据不可找回;若想留档,先执行:
--   SELECT * FROM todos;  -- 在 Console 里看一眼/导出,再删
-- ============================================================

DROP TABLE IF EXISTS todos;
