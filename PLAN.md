# MyCal — 日常工作台 · 项目规划

> 一个「日历 + 每日待办」的本地 Web 应用:看月历,点哪天,就管哪天的事。

## 1. 项目目标

做一个供个人日常工作使用的小型 Web 应用:

- **月历视图**:按月展示,能翻上/下月,能一键回到今天;
- **点击某天 → 弹出/展示当天的待办列表**:可添加、勾选完成、删除待办;
- **数据留存**:刷新、关掉浏览器再打开,数据还在;
- **轻量、快、离线可用**:纯本地运行,不依赖任何后端。

## 2. 功能范围

### v1(本次实现)

| 模块 | 功能点 |
| --- | --- |
| 日历 | 月视图网格(周一打头)、上/下月切换、回到今天 |
| 日历 | 「今天」高亮、选中日高亮、有待办的日期显示小圆点 |
| 待办面板 | 点击日期后在右侧面板展示:日期标题、待办列表 |
| 待办操作 | 添加(回车或点按钮)、勾选完成/取消、删除 |
| 待办面板 | 实时统计「共 x 项 · 完成 y 项」,空状态提示 |
| 持久化 | localStorage 存储,按日期分桶,刷新不丢 |
| 响应式 | 宽屏:日历 + 右侧面板双栏;窄屏:面板变成底部抽屉 |

### v2(后续可选,不在本次范围)

- 待办编辑文字、优先级/颜色标记;
- 跨月拖拽/复制待办;搜索全部待办;
- 周视图、聚合「今日待办」速览栏;
- 导入/导出 JSON 备份;多标签(工作/生活);
- 如需多设备同步,再引入后端或文件同步方案。

## 3. 技术方案

- **构建**:Vite 5 + React 18 + TypeScript(严格模式);
- **样式**:原生 CSS(自定义属性做主题色),不引 UI 库,保持零冗余依赖;
- **状态**:React `useState` + `useMemo`,不引入状态库;
- **持久化**:自定义 `useTodos` hook,读写 `localStorage`
  (存储键 `mycal:todos:v1`,结构为 `{ "YYYY-MM-DD": Todo[] }`);
- **日期计算**:自己写 `src/utils/date.ts`(格式化、月矩阵、周起始),不依赖 dayjs 等库——逻辑量很小,自写可全程类型安全。

### 数据模型

```ts
interface Todo {
  id: string;        // crypto.randomUUID()
  text: string;      // 待办内容
  done: boolean;     // 是否完成
  createdAt: number; // 创建时间戳
}

// localStorage: { "2025-11-26": Todo[], ... }
type TodoStore = Record<string, Todo[]>;
```

## 4. 目录结构

```
mycal/
├── PLAN.md                 # 本文档
├── index.html
├── package.json / tsconfig*.json / vite.config.ts
└── src/
    ├── main.tsx            # 入口
    ├── App.tsx             # 布局 + 全局状态(当前月、选中日、待办数据)
    ├── types.ts            # Todo / TodoStore 类型
    ├── utils/date.ts       # 日期工具:fmtKey / 月矩阵 / 星期表
    ├── hooks/useTodos.ts   # 待办状态 + localStorage 持久化
    ├── components/
    │   ├── Calendar.tsx    # 月历网格
    │   ├── DayPanel.tsx    # 当日待办面板(添加/勾选/删除)
    │   └── DayCell.tsx     # 单元格(今天/选中/圆点标记)
    └── styles.css          # 全部样式(桌面双栏 + 移动抽屉)
```

## 5. 交互设计

1. 打开应用 → 默认当月,默认选中「今天」;
2. 点任意日期 → 右侧面板切换为该天,展示其待办;
3. 输入框输入 + 回车(或点「添加」)→ 待办入列;空内容不提交;
4. 点复选框切换完成态(划线样式);点 🗑 删除,删除该天最后一项后面板显示空状态;
5. 点数字(如 ‹ ›)翻月;点「今天」回到当月并选中今天;
6. 有待办的日期格子上显示一个小圆点,一眼看出哪天有活儿。

## 6. 里程碑

1. ✅ 规划文档(本文档)
2. 界面草图(Excalidraw 手绘风)
3. 项目骨架(Vite/TS 配置、入口、类型)
4. 核心组件(日历 + 待办面板 + 持久化)
5. 构建验证(npm install + `npm run build` 通过)
6. 启动 dev server,浏览器实测交互
